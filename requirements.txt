Django==5.2.17
# Resend's API, as a Django email backend. The [resend] extra adds the
# signature verification Anymail uses for Resend's status webhooks.
django-anymail[resend]==15.2
gunicorn==23.0.0
openpyxl==3.1.5
psycopg[binary]==3.3.4
pwned-passwords-django==5.2.0
python-dotenv==1.2.2
whitenoise==6.12.0
